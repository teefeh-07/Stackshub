/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    /**
     * Tailwind CSS v4 PostCSS plugin.
     */
    "@tailwindcss/postcss": {},
  },
};

export default config;
